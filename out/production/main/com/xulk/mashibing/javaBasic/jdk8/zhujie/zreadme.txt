

注解是一种元数据形式。即注解是属于java的一种数据类型，和类、接口、数组、枚举类似。
注解用来修饰，类、方法、变量、参数、包。
注解不会对所修饰的代码产生直接的影响。

注解其实就是一种标记，可以在程序代码中的关键节点（类、方法、变量、参数、包）上打上标记
然后程序在编译时或运行时可以检测到这些标记从而执行一些特殊操作


定义注解->相当于定义标记
配置注解->把标记打在需要用到的程序代码中
解析注解->在编译期或运行期间检测到标记，执行特殊操作

在底层实现上，所有的自定义注解都会自动继承java.lang.annotation.Annotation接口
